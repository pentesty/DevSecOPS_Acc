# API
USERNAME: str = 'eve.holt@reqres.in'
PASSWORD: str = 'cityslicka'
URI: str = 'https://reqres.in/api'

# CLI
HOST_NAME: str = ''
HOST_USERNAME: str = ''  # Specify the default username where the user needs to run the command
HOST_PASSWORD: str = ''  # Specify the default password where the user needs to run the command
